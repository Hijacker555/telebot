name: Deploy Python App

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Deploy
      run: |
        ssh root:212.86.108.148 'git clone https://github.com/Hijacker555/telebot/blob/711b2deca5208a49e3acc69fd87a3c26c211879f/chatgpt.py'
    - name: Restart server
      run: |
        ssh root:212.86.108.148 'python3 chatgpt.py &'
